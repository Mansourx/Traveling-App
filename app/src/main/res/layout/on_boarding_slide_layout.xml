<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
        android:id="@+id/image_view"
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:src="@drawable/group_10"
        android:layout_centerInParent="true"
        android:layout_alignParentTop="true"
        android:layout_margin="70dp"/>

    <TextView
        android:id="@+id/heading_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Heading"
        android:layout_margin="20dp"
        android:textSize="20sp"
        android:fontFamily="@font/bungee"
        android:textColor="#FFFFFF"
        android:layout_below="@+id/image_view"
        android:layout_centerInParent="true"/>

    <TextView
        android:id="@+id/description_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="80% of sales commission calculations contain errors (a process that's historically been managed in spreadsheets). That's why we are excited to partner with CaptivateIQ as they help companies modernize sales commissions with flexibility, transparency, and automation."
        android:layout_margin="20dp"
        android:padding="10dp"
        android:gravity="center"
        android:textColor="#FFFFFF"
        android:lineSpacingExtra="5dp"
        android:textSize="16sp"
        android:fontFamily="@font/antic"
        android:layout_below="@+id/heading_text"
        android:layout_centerInParent="true"/>


</RelativeLayout>